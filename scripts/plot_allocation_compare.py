#!/usr/bin/env python
"""Figure: three-way allocation comparison (uniform vs. fixed-asymmetric vs. derived).

Plan Week 7. Reads the table from experiments/03_allocation_compare.py and saves
results/figures/allocation_compare.png. Runs GPU-free from committed results.
"""

OUT = "results/figures/allocation_compare.png"


def main() -> None:
    # TODO(Week 7): grouped bars of ppl/LongBench vs avg-bits by scheme, savefig(OUT).
    raise NotImplementedError("Week 7: allocation-comparison figure")


if __name__ == "__main__":
    main()
