# Raw run outputs — committed so figures regenerate without a GPU (Plan Week 8).

- `stats/`   per-layer/head statistics dumps (JSON)
- `figures/` generated figures; every one is the output of a `scripts/` script (`make figures`)
